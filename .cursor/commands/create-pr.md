# Create Pull Request

## Description

Create a new pull request on GitHub for the current branch. Only create if a PR doesn't already exist for this branch.

## Steps

1. Check if a PR already exists for the current branch
2. If no PR exists:
   - Create a new PR on GitHub
   - Set appropriate title and description
   - Set base branch (typically main)
3. If PR already exists, inform user
